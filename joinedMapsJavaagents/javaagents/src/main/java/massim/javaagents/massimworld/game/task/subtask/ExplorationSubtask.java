package massim.javaagents.massimworld.game.task.subtask;

import eis.iilang.Action;
import massim.javaagents.massimworld.agent.MassimTeam4Agent;
import massim.javaagents.massimworld.map.Direction;

public class ExplorationSubtask extends Subtask {

    Direction direction = null;//super.getDirection();

    @Override
    public Action getAction(MassimTeam4Agent agent) {
//        if (agent.getMap().getWallThicknes(agent, direction) < 4) {
//            return new Action()
//        } else {
//            return (new Action());
//        }
        // TODO
        return null;
    }

}
